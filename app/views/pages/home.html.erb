<header class="bg-white">	
	<div class="container-fluid">
		<div class="row">
			<div class="col-sm-12 height-100-vh d-flex justify-content-center align-items-center">
				<div>
					<%= image_tag "logo.png", :width => "500", class: "mb-5 img-fluid" %>
					<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
					  <div class="form-inputs">
					    <%= f.input :email, label: false, placeholder: "usuário", autofocus: true%>
					    <%= f.input :password, label: false, placeholder: "senha" %>
					  </div>
					  <div class="form-actions text-right">
					    <%= f.button :submit, "ENTRAR", class: "btn aud-bg-color text-white" %>
					  </div>
					<% end %>
					<% if user_signed_in? %>
          	<%= link_to "Sair", destroy_user_session_path, class: 'btn btn-danger btn-block', method: :delete %>
          <% end %>
				</div>
			</div>
		</div>
	</div>
</header>